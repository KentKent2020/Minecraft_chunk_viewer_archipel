import { registerLoaders } from '@loaders.gl/core';
import { ImageLoader } from '@loaders.gl/images';
import { global } from 'probe.gl/env';
import log from '../utils/log';
import { register } from '../debug';
import jsonLoader from '../utils/json-loader';
var version = typeof "8.0.0-beta.2" !== 'undefined' ? "8.0.0-beta.2" : global.DECK_VERSION || 'untranspiled source';

if (global.deck && global.deck.VERSION !== version) {
  throw new Error("deck.gl - multiple versions detected: ".concat(global.deck.VERSION, " vs ").concat(version));
}

if (!global.deck) {
  if (process.env.NODE_ENV !== 'production') {
    log.log(0, "deck.gl ".concat(version, " - set deck.log.level=1 (or higher) to trace attribute updates"))();
  }

  global.deck = global.deck || {
    VERSION: version,
    version: version,
    log: log,
    _registerLoggers: register
  };
  registerLoaders([jsonLoader, ImageLoader]);
}

export default global.deck;
//# sourceMappingURL=init.js.map